SELECT CONCAT(firstname,lastname)
FROM personal_info;

SELECT CONCAT(
    CONCAT(firstname,'_'), lastname)
    FROM personal_info;

 SELECT CONCAT(username, platform) AS details
 FROM social_media;
 

